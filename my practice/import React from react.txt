import React from 'react';
import { ArrowLeft } from '../../assets/icons/SvgIconsList';
import SearchBar from '../SelectProfessionalModalComponents/SearchBar';
import CategoryGrid from '../SelectProfessionalModalComponents/CategoryGrid';
import CategoryItem from '../SelectProfessionalModalComponents/CategoryItem';

// ProfessionSelector Component
const ProfessionSelector = () => {
  return (
    <main className="p-6 font-outfit h-screen overflow-hidden">
      <section className="bg-white p-4 rounded-md shadow-lg">
        <header className="flex items-center mb-4">
          
          <ArrowLeft className="w-6 h-6 text-gray-600 mr-4 cursor-pointer" />

          <h1 className="text-2xl font-outfit font-bold">Select Profession</h1>
        </header>

        <SearchBar />
        
        <h2 className="text-black text-xl font-outfit mt-12 mb-4 ml-56">Popular Categories</h2>
        
        <CategoryGrid />
      </section>
    </main>
  );
};


export default ProfessionSelector;


    import React from "react";
import { CarpenterIcon, ChefIcon, ConstructionIcon, DeliveryBoyIcon, DriverIcon, ElectricianBoyIcon, MaidIcon, MechanicIcon, PainterIcon, PlumberBoyIcon, WelderIcon, WorkerIcon } from '../../assets/icons/SvgIconsList';
import CategoryItem from "./CategoryItem";

const CategoryGrid = () => {
    const categories = [
      { name: 'Maid', img: <MaidIcon /> },
      { name: 'Delivery Boy', img: <DeliveryBoyIcon /> },
      { name: 'Chef', img: <ChefIcon /> },
      { name: 'Electrician', img: <ElectricianBoyIcon /> },
      { name: 'Mechanic', img: <MechanicIcon /> },
      { name: 'Driver', img: <DriverIcon /> },
      { name: 'Painter', img: <PainterIcon /> },
      { name: 'Construction', img: <ConstructionIcon /> },
      { name: 'Plumber', img: <PlumberBoyIcon /> },
      { name: 'Carpenter', img: <CarpenterIcon /> },
      { name: 'Welder', img: <WelderIcon /> },
      { name: 'Worker', img: <WorkerIcon /> },
    ];

    return (
      <div className="w-full flex justify-center mt-4 mb-0">
        <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-6 gap-5 max-w-[871px] mx-auto">
          {categories.map((category, index) => (
            <CategoryItem key={index} name={category.name} icon={category.img} />
          ))}
        </div>
      </div>
    );
};

export default CategoryGrid;
import React from "react";
const CategoryItem = ({ name, icon }) => {
    return (
      <div className="flex flex-col items-center flex-1">
        <div className="w-32 h-32 flex items-center justify-center bg-white rounded-lg border border-gray-300 mb-2">
          {icon}
        </div>
        <div className="text-center mt-2.5">{name}</div>
      </div>
    );
  };

  export default CategoryItem;
  
  
import React from "react";
import { SearchIcon } from "../../assets/icons/SvgIconsList";

const SearchBar = () => {
    return (
      <form className="flex items-center rounded-full overflow-hidden border border-gray-300 shadow-md bg-white max-w-[700px] mx-auto">
        <div className="flex items-center flex-1 ml-2">
          <SearchIcon className="ml-4 text-gray-500" />
          <input  
            type="text"
            placeholder="Search for a profession"
            aria-label="Search for a profession"
            className="flex-1 px-4 py-2 border-0 rounded-full outline-none"
          />
        </div>
        <button type="submit" className="px-6 py-2 text-white bg-primary rounded-full mt-2 mb-2 mr-2">
          Search
        </button>
      </form>
    );
  };

export default SearchBar;

